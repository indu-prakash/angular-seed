{
  "name": "angular-seed",
  "private": true,
  "version": "0.0.0",
  "description": "A basic skeleton for any angular application.",
  "devDependencies": {
    "bower": "^1.7.7",
    "http-server": "^0.9.0"
  },
  "scripts": {
    "postinstall": "bower install",
    "start": "http-server -a localhost -p 8000 -c-1 ./app"
  }
}
